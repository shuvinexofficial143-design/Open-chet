import {test,expect} from '@playwright/test';
test('browse category rails, filter, inspect and send multiple cards without leaving chat',async({page},info)=>{
const errors:string[]=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('/?demo=1');await page.getByRole('button',{name:/Dr. Arjun Mehta.*Can you share/}).click();await page.getByRole('button',{name:'Open catalogue',exact:true}).click();const dialog=page.getByRole('dialog');await expect(dialog.getByRole('heading',{name:'Catalogue',exact:true})).toBeVisible();await expect(dialog.getByRole('region',{name:'Injection',exact:true})).toBeVisible();const rail=dialog.getByLabel('Injection product rail',{exact:true});const widths=await rail.evaluate(e=>({scroll:e.scrollWidth,client:e.clientWidth}));expect(widths.scroll).toBeGreaterThan(widths.client);await dialog.getByRole('button',{name:'More Injection products'}).click();await expect.poll(()=>rail.evaluate(e=>e.scrollLeft)).toBeGreaterThan(0);
await dialog.getByLabel('Search catalogue',{exact:true}).fill('ceftriaxone');await expect(dialog.locator('.catalogue-product')).toHaveCount(1);await dialog.getByRole('button',{name:'View details'}).click();await expect(dialog.getByRole('heading',{name:'Ceftriaxone',exact:true})).toBeVisible();await expect(dialog.getByText('1 g',{exact:false})).toBeVisible();await dialog.getByRole('button',{name:'Back to catalogue'}).click();await dialog.getByLabel('Search catalogue',{exact:true}).fill('');await dialog.getByLabel('Catalogue Category',{exact:true}).selectOption('Tablet');await expect(dialog.locator('.catalogue-category')).toHaveCount(1);await dialog.getByRole('checkbox',{name:'Select Pantoprazole tablets'}).check();await dialog.getByRole('checkbox',{name:'Select Ofloxacin tablets'}).check();await dialog.getByRole('button',{name:'Send selected (2)'}).click();await expect(dialog.getByRole('status')).toContainText('2 products added to demo chat');await dialog.getByRole('button',{name:'Close dialog'}).click();await expect(page.locator('.shared-product')).toHaveCount(2);await expect(page.getByRole('button',{name:'Resume AI',exact:true})).toBeVisible();await page.reload();if(info.project.name==='mobile')await page.getByRole('button',{name:/Dr. Arjun Mehta.*Ofloxacin|Dr. Arjun Mehta.*Pantoprazole/}).click();await expect(page.locator('.shared-product')).toHaveCount(2);expect(errors).toEqual([]);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=window.innerWidth)).toBe(true);
await page.getByRole('button',{name:'Open catalogue',exact:true}).click();await page.screenshot({path:`test-results/catalogue-${info.project.name}.png`});
});
